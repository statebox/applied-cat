import React from 'react'

// full page shown when `isInitialLoading` is true in `../use-auth.js`

export default function FullPageLoading() {
    return (
        <div id="fullpage-loading">Loading...</div>
    )
}